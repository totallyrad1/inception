version: '3'

services:
    mariadb:
        build: ./requirements/mariadb
        volumes:
            - inception-vol:/var/lib/mysql
        networks:
            - inception-net
        env_file:
            - .env
    wordpress:
        build: ./requirements/wordpress
        volumes:
            - inception-vol:/var/www/inception
        networks:
            - inception-net
        depends_on:
            - mariadb
        env_file:
            - .env
    nginx:
        build: ./requirements/nginx
        volumes:
            - inception-vol:/var/www/inception
        networks:
            - inception-net
        depends_on:
            - wordpress
        ports:
            - "80:80"
            

networks:
    inception-net:
        name: inception
        driver: bridge

volumes:
    inception-vol: